import styled from "styled-components"

export const Container = styled.div`
    .header {
        background: var(--tertiary);
        height: 5rem;
    }

    .body {
        padding: 1rem 2rem;
    }
`;